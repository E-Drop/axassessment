import React from 'react';
import styled from 'styled-components/macro';

const NavigationTitle = styled.h3`
color: #fff;
`;

const Title = () => {
  return (
    <NavigationTitle>Welcome to Gnome App!</NavigationTitle>
  );
}

export default Title
